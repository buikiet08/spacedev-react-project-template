import styled from "styled-components";

export const ShortMoreStyle = styled.div`
    .content{
        overflow:hidden;
    }
    .read-more{
        padding-top:10px;
        display:flex;
        justify-content:center;
        align-items:center;
        width:100%;
    }
`